
import React from 'react' 

const input = props => {
    let inputElement = null;

    switch (props.inputType) {
        case "input":
          inputElement = <input 

          />    
    }
}

export default input
import React, { Component } from "react";

class Auth extends Component {
  state = {
    token: null,
    userId: null,
    loading: null,
    error: null,
  };

  render() {
    // let form = this.state
    return (
    <div>
     
    </div>)
  }
}

export default Auth;

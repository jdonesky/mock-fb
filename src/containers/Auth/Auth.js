import React, { Component } from "react";

class Auth extends Component {
  state = {
    token: null,
    userId: null,
    loading: null,
    error: null,
  };

  render() {
    return <div></div>;
  }
}

export default Auth;
